#include <iostream>
#include <stack>
#include <mpi.h>

using namespace std;

int main(int argc,  char* argv[]){
    stack<string> images;

    //push all files in folder onto stack
    
    while (!images.empty()) {
        // while no new messages
        // send(images.
    }
}

